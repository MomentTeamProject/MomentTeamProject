<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.user.mapper">

	<insert id="insert">
		insert into m_user(u_userid, u_userpw, u_name, u_nick)
		values (#{userid}, #{userpw}, #{username}, #{usernick})
	</insert>

	<select id="idcheck" resultType="integer">
		select count(*) from m_user where u_userid=#{userid}
	</select>	
	
	<select id="nick_check" resultType="integer">
		select count(*) from m_user where u_nick=#{usernick}
		
	</select>	
	
	
	<select id="login" resultType="web.user.WebUserVO">
		select * from m_user where u_userid=#{userid} and u_userpw=#{userpw}
	</select>
	
	<!-- 회원 인증키(난수) 생성 -->
	<insert id="createAuthKey" parameterType="Map">
		update m_user set u_authkey=#{authKey} where u_userid=#{userid}
	</insert>
	<!-- 회원 인증키(난수) Y로 변경 -->
	<update id="userAuth" >
		update m_user set u_admin = 'Y' where u_userid = #{userid} and u_authkey = #{authKey}
	</update>
</mapper>
 